extends Node2D

func _ready():
	"""
	var win = JavaScript.get_interface('window')
	var ply = JSON.parse(win.player).result
	for x in ply:
		var spt = Sprite.new()
		spt.texture = load("res://icon.png")
		spt.position.x = x.x ; spt.position.y = x.y
		self.add_child(spt)
		pass
	"""
	pass 
